name: Check Uptime

on:
  workflow_dispatch:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  uptime_check:
    name: Uptime Check
    runs-on: ubuntu-latest
    steps:
      - name: Check endpoint
        run: |
          curl --fail https://leetcode-streaker.onrender.com/api/streak/aravindhprabu2005 || exit 1
